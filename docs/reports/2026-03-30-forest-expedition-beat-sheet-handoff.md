# 2026-03-30 Forest Expedition Beat Sheet Handoff

## Scope

Scout handoff for `ECO-20260330-scout-52`: ground the first deeper expedition chapter around the live forest cave and climbable spaces.

## Strongest Expedition

Recommended first expedition:

- a forest-first chapter through `root-hollow` into `log-run`

Why this is the right fit:

- the geometry is already the deepest traversal space in the game
- the chapter can feel richer without changing genre
- the science language is already strong around moisture, decomposition, shelter, and clustered life

## Best Chapter Shape

This should not read like three normal route beats.

It should read like one longer outing with three connected legs:

### 1. Lower Hollow

Core feeling:

- leave the flat forest trail
- drop into the sheltered lower lane
- notice that the hollow is wetter, dimmer, and fuller of cover

Best live supports:

- `banana-slug`
- `sword-fern`
- `redwood-sorrel`
- `ensatina`
- `Hollow Moisture`

### 2. Trunk Climb

Core feeling:

- use the climbable trunks as the expedition’s special movement identity
- move from lower moisture shelter up toward ledges and higher pockets

Best live supports:

- `root-hollow-fir-trunk`
- `root-hollow-cave-trunk`
- `tree-lungwort`
- `licorice-fern`

Important read:

- this is the expedition’s key “more game-like” step, so it must stay forgiving and visible

### 3. Upper Run

Core feeling:

- emerge back into `log-run`
- see how the chapter reconnects with the regular forest route from a higher, drier, more open angle

Best live supports:

- `log-run-fir-trunk`
- `licorice-fern`
- `fir-cone`
- existing `forest-seed-travel` timing language if a notebook cue is needed later

## Best Main-Agent Scope For `main-84`

The cleanest expedition-shell pass is:

1. define one expedition chapter in the new station slot
2. activate a simple three-leg chapter through lower hollow, climb, and upper run
3. reuse the live forest geometry and species instead of inventing a new biome or cave engine
4. make the chapter a little longer than a route beat, but still calm and kid-friendly

## Content Matrix

| Leg | Space | Main lesson | Strongest live supports |
| --- | --- | --- | --- |
| Lower Hollow | `root-hollow` floor and sill | moisture and shelter cluster life | `banana-slug`, `ensatina`, `tree-lungwort`, `Hollow Moisture` |
| Trunk Climb | climbable trunks plus ledges | height changes what feels sheltered and reachable | trunk climbs, `tree-lungwort`, `licorice-fern` |
| Upper Run | `log-run` re-entry | the forest opens without fully losing cover | `licorice-fern`, `fir-cone`, upper ledges, `forest-seed-travel` |

## Guardrails

- Do not add danger, damage, or fail-state traversal.
- Do not turn the expedition into a long checklist.
- Do not introduce a heavy inventory gate.
- Keep each leg readable at `256x160`.
- Let the existing forest geometry do most of the work.

## Queue Guidance

- `ECO-20260330-scout-52` can close with this handoff.
- `ECO-20260330-main-84` should now move forward as one connected forest expedition shell built from live `root-hollow` and `log-run` spaces.
