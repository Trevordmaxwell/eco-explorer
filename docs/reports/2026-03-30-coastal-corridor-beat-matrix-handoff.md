# 2026-03-30 Coastal Corridor Beat Matrix Handoff

## Scope

Scout handoff for `ECO-20260330-scout-50`: prepare the quiet authored coastal corridor pass for `beach <-> coastal-scrub` and `coastal-scrub <-> forest`.

## Current Read

The coastal seams are already functional and science-safe, but they still read more like generic blends than the inland links now do.

The right next move is not another corridor system. It is one calm authored pass that gives each coastal seam a clearer carrier story.

## Best Beat Plan

### 1. `beach <-> coastal-scrub` should stay carrier-first only

This seam already has the right exposed-to-sheltered shape. It does not need a prompt.

Best additions:

- keep `beach-grass` and `sand-verbena` as the last open-dune carriers
- let `dune-lupine` arrive a little more assertively just after the threshold
- add the first sturdier shrub signal with one light `pacific-wax-myrtle` arrival on the scrub-owned side
- keep `coyote-brush` as the far-right accent, not the main teaching carrier

Why this is the right fit:

- it teaches pioneer-to-cover drift without clutter
- it reinforces the live scrub route beat around sturdier cover
- it avoids turning the front-half seam into another notebook stop

### 2. `coastal-scrub <-> forest` should carry the shared middle-edge anchor

This seam is the better place for the one notebook window in the coastal pass.

Best additions:

- add `salmonberry` as the key shared transition carrier
- add one small `nurse-log` anchor near the forest-owned side
- keep `sword-fern` and the first young conifer shapes already doing the moisture-and-shade drift

This gives the seam a stronger route-facing identity:

- scrub cover thickens
- the first real shade appears
- `salmonberry` bridges the coastal and forest edge chapter

### 3. Use the existing `coastal-edge-shade` prompt as the one quiet notebook window

The current prompt seed is already the right question:

- `Where does the scrub start holding forest-like shade?`

It is currently underused because the corridor does not yet carry the full local trio that supports it.

Best main-agent move:

- make the `forest-edge` side of `coastal-scrub <-> forest` reliably capable of surfacing `coastal-edge-shade`
- add one matching field-partner cue so the seam gets one quiet authored line instead of a new corridor UI layer

Suggested cue tone:

- `The first shade here is still scrub-made, not full forest yet.`

## Guardrails

- Do not add another corridor overlay or route board.
- Do not deepen the beach seam with tide-line or wrack content.
- Do not push the coastal-forest seam into deep forest darkness too early.
- Keep the prompt work to one seam only.
- Let the carriers do most of the teaching.

## Main-Agent Targets For `main-81`

The cleanest implementation bundle is:

1. strengthen `beach <-> coastal-scrub` with a clearer `dune-lupine` to `pacific-wax-myrtle` handoff
2. add `salmonberry` plus a small `nurse-log` anchor to `coastal-scrub <-> forest`
3. let that second seam surface `coastal-edge-shade`
4. add one matching field-partner cue for that prompt and stop there

## Queue Guidance

- `ECO-20260330-scout-50` can close with this handoff.
- `ECO-20260330-main-81` should now move forward as a carrier-first coastal continuity pass with one quiet scrub-to-forest notebook window.
