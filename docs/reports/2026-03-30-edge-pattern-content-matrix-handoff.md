# 2026-03-30 Edge Pattern Content Matrix Handoff

## Scope

Scout handoff for `ECO-20260330-scout-45`: recommend the smallest content-fuel pack that makes `Edge Pattern Line` feel like a real transition chapter instead of only a route shell.

## Current Read

`Edge Pattern Line` already has the right route structure:

- a scrub-side pioneer-to-cover beat
- a forest middle edge beat
- a treeline low-fell finish

What it still needs is not another system. It needs stronger content doing route work inside those three beats.

The strongest route-fuel path is:

1. make the scrub beat lean harder on `pacific-wax-myrtle` and sturdier shrub cover
2. use `salmonberry` as the route's clearest shared transition anchor across `coastal-scrub` and `forest`
3. give the treeline finish one clearer bridge note from tree-shaped cover into low fell growth

## Beat Matrix

### Beat 1. `Scrub Pattern`

Current supports already live:

- request entries: `dune-lupine`, `pacific-wax-myrtle`, `salmonberry`
- ecosystem notes:
  - `Shelter Builds Here`
  - `Swale Shelter`
  - `Edge Moisture`
- prompt seeds:
  - `coastal-ready-patch`
  - `coastal-swale-shelter`
  - `coastal-edge-shade`
- nursery clue:
  - `dune-lupine`

Main gap:

- the beat is structurally correct, but `pacific-wax-myrtle` is the one clue here that can make the beat feel newly authored instead of only reusing coastal-line leftovers

Best content addition:

- add one compact note or prompt support that explicitly teaches sturdier shrub cover building out of dune pioneers
- best local trio:
  - `dune-lupine`
  - `pacific-wax-myrtle`
  - `coyote-brush` or `salmonberry`

Recommendation:

- prefer a new note-backed support around `windbreak-swale` or `shrub-thicket`, not a new species

### Beat 2. `Cool Edge`

Current supports already live:

- request entries: `salmonberry`, `sword-fern`, `redwood-sorrel`
- ecosystem note:
  - `Creekside Shelter`
- prompt seed:
  - `forest-cover-loosens`
- comparison-ready shared entries:
  - `salmonberry`
  - `sword-fern`
- nursery clue:
  - `salmonberry`

Main gap:

- the beat has the right species, but it still needs one stronger route-facing bridge between scrub edge and forest cover so this middle chapter does more than inherit the earlier moisture logic

Best content addition:

- deepen `salmonberry` as the route's shared transition anchor between `coastal-scrub` and `forest`
- use one route-aware note or prompt that makes the player compare thicket shelter with cool forest edge cover instead of treating them as separate facts

Recommendation:

- main should not spend this pass on new comparison plumbing; the existing comparison path for `salmonberry` and `sword-fern` is already enough
- instead, add one stronger forest-side note or prompt moment keyed to `salmonberry`, `sword-fern`, and `redwood-sorrel`

### Beat 3. `Low Fell`

Current supports already live:

- request entries: `krummholz-spruce`, `dwarf-birch`, `mountain-avens`
- ecosystem notes:
  - `Wind Shapes Trees`
  - `Fell Bloom Window`
  - `Low Ground Wins`
- prompt seeds:
  - `treeline-protected-shapes`
  - `treeline-fell-bloom`
- nursery clue:
  - `mountain-avens`

Main gap:

- the treeline finish currently has good pieces, but they still read like two adjacent lessons:
  - bent tree shapes
  - low fell bloom

Best content addition:

- add one explicit bridge note that ties `krummholz-spruce`, `dwarf-birch`, and `mountain-avens` into one transition story from tree-shaped cover to low exposed ground

Recommendation:

- prefer one new treeline ecosystem note over another prompt-only seed here, because the route finish wants one durable chapter summary more than another rotating question

## Strongest Main-Agent Slice For `main-76`

The cleanest implementation pack is:

1. one new coastal-scrub transition note or prompt centered on sturdier shrub cover
2. one forest-side route-aware note or prompt that makes `salmonberry` the middle-edge anchor
3. one new treeline bridge note that ties tree-shape loss to low fell bloom
4. only small supporting prompt or comparison tweaks if those notes need them

That keeps `main-76`:

- transition-driven
- notebook-first
- science-safe
- small enough to review cleanly

## Guardrails

- Do not solve this by adding a lot of new species.
- Do not add another route mechanic, reward layer, or station board row.
- Do not turn nursery clue text into in-biome chatter or required checklist language.
- Let shared comparisons stay secondary to the active route board and notes.

## Queue Guidance

- `ECO-20260330-scout-45` can close with this report.
- `ECO-20260330-main-76` should now be promoted to `READY`.
- `main-76` should aim for one note-first content-fuel pass, not a broad content expansion.
