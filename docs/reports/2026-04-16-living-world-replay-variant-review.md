# Living-World Replay Variant Review

Reviewed `ECO-20260416-critic-306` for lane 4.

## Result

No blocking findings.

## What Checked Out

- The replay windows now change what the player can actually log, not just what the route is called.
- `forest-moisture-holders` keeps its filed identity stable while `Moist Hollow` adds route-local damp alternates that already belong in Root Hollow.
- `treeline-low-fell` uses `moss-campion` as a peak-phenology bloom alternate without widening the route shell or changing the filed note path.
- The shared seam change stayed compact: `worldStateFocus` now adds alternate slot carriers through the same evidence-slot matching path already used by process-backed routes.
- Focused controller/runtime checks and the build all passed, so the pass is stable enough to unblock the next chapter-planning scout step.

## Watch Item

If future replay variants add more alternate carriers, keep them semantically tight to the slot label. This pass works because `seep-moss-mat`, `tree-lungwort`, and `moss-campion` still read like believable habitat clues for their existing slots rather than a second route layered on top.
