# 2026-03-30 Comparison And Close-Look Candidates Handoff

## Scope

Scout handoff for `ECO-20260330-scout-56`: prepare the next lane-2 journal-richness step after the coastal and alpine content packs.

## Current Read

The content-richness lane now has enough authored material to justify one journal-facing payoff pass, but the safest version is still narrow.

What is already live and strong:

- the comparison system is same-pane, note-backed, and already proven with a small allowlist
- close-look is already a compact visual-first vignette, not a second encyclopedia
- the coastal-front pack added one strong new shared thicket carrier and one strong new visual collectible
- the alpine pack added one strong new shared berry anchor plus denser note-backed alpine structure

The next pass should not try to turn every new entry into both a comparison and a close-look target. The strongest move is one ready comparison addition plus two visual-first close-look additions.

## Comparison Candidates

### Ready now: `nootka-rose`

This is the clearest new shared-species comparison entry that is already fully note-backed on both sides.

Why it is ready:

- `coastal-scrub` now has `Thorny Cover`
- `forest` now has `Edge Berry Thicket`
- both notes teach real habitat contrast instead of repeating the entry fact text

What it would teach:

- scrub side: thorny stems as safer cover in a brighter, more open thicket
- forest side: a shadier berry-thicket stack just inside the first forest edge

Why it fits `main-88`:

- no new UI is needed
- no new save state is needed
- the comparison can reuse the same note-backed card pattern as the live entries

### Not ready yet: `lingonberry`

`lingonberry` is now a great shared alpine carrier, but it is not yet comparison-ready under the current note-backed rule.

Why not yet:

- `treeline` now has `Heath Berry Mats`, which supports it locally
- `tundra` does not yet have a local ecosystem note that includes `lingonberry`
- the live `tussock-ground` note is about sedge structure and lemming cover, not berry mats

Recommendation:

- do not loosen the comparison rule just to force `lingonberry` into the pane now
- if `main-88` has any extra room, at most author one tiny tundra berry-ground note first
- otherwise defer `lingonberry` comparison to the later content wave

## Close-Look Candidates

### First add: `sand-dollar-test`

This remains the strongest close-look addition from the recent coastal content wave.

Why it is the best first pick:

- the sprite silhouette is distinct and readable
- the object naturally supports simple callouts
- it teaches a real “body clue left by the sea” idea without extra system work

Suggested callout direction:

- star pattern
- petal grooves

### Second add: `lingonberry`

This is the strongest alpine close-look candidate from the new pack.

Why it beats the other alpine options:

- berries plus evergreen leaves read better at enlarged scale than sedge structure
- it gives the alpine half one more visual reward instead of leaving all close-look attention on the coast
- it complements the shared-entry role without requiring comparison mode to carry all of the learning payoff

Suggested callout direction:

- berry cluster
- evergreen leaves

### Hold for later: `white-arctic-mountain-heather`

This is a plausible later close-look entry, but it is a weaker first addition than `lingonberry`.

Why to wait:

- its mat-structure teaching is strong, but the visual payoff is subtler
- the current pass should favor the entries most likely to feel obviously rewarding on first reveal

## Recommended `main-88` Shape

Keep `main-88` compact:

1. add `nootka-rose` to the comparison allowlist and tests
2. keep the existing live comparison entries as-is
3. add close-look payload seeds for `sand-dollar-test` and `lingonberry`
4. add only the smallest supporting content needed for those entries, if any

## What `main-88` Should Not Do

- do not widen comparison support to every shared entry
- do not add more than one new comparison entry in this pass
- do not loosen the note-backed comparison rule for `lingonberry`
- do not turn close-look into a larger overlay or journal mode
- do not reopen station, route, prompt, or progression systems

## Queue Guidance

- `ECO-20260330-scout-56` can close with this handoff.
- `ECO-20260330-main-88` can move to `READY` once this report is linked because `critic-64` is already complete.
