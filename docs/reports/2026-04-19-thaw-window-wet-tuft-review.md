# 2026-04-19 Thaw Window Wet-Tuft Review

Reviewed `ECO-20260419-critic-318` for lane 4.

## Result

No blocking issue found.

The implementation stays inside the intended lane-4 seam:

- `Thaw Window` gets a player-felt middle-leg consequence instead of more shell or filing copy
- canonical `Short Season` filing and the `Thaw Window` display stamp stay stable
- no second tundra route, new support row, or broader replay framework appears
- the change remains data-first by extending `processFocus` rather than adding route-specific controller branching

## What Looks Good

- The request test proves the live rule change directly: once `first-bloom` is logged, active `Thaw Window` can treat `bigelows-sedge` as `wet-tuft` while the filed identity still resolves through the existing clue-backed note seam.
- The controller proof keeps the support split sharp: `hand-lens` retargets to the active middle-leg carrier, marks the hint as `support-biased`, and leaves the ordinary `Notebook fit:` language in place for `cottongrass`.
- The runtime proof uses the live thaw-skirt shelf and keeps the comparison honest by showing `note-tabs` does not auto-snap to `bigelows-sedge` in the same setup.

## Watch Item

- `activeSlotEntryIdsBySlotId` remains slot-wide for `assemble-evidence` routes, so future middle-leg alternates will also inherit that broader per-slot reach unless lane 4 adds slot-zone gating at the request-definition layer. If a later route needs an alternate tied to one exact shelf, solve that in request metadata rather than in controller exceptions.

## Verification

- `npx vitest run src/test/field-requests.test.ts src/test/field-request-controller.test.ts src/test/runtime-smoke.test.ts -t "Thaw Window|wet-tuft|bigelows-sedge|thaw-window bloom"`
