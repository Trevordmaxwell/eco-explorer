# 2026-04-06 Support Cue Copy Review

## Findings

No blocking findings.

## Review Notes

The cue lands in the right seam.

- It reuses the inspect bubble's existing accent line instead of growing a route strip, season strip, or notebook surface.
- The stronger `LENS CLUE` label is correctly reserved for the active hand-lens winner, while ordinary notebook fits keep the calmer legacy wording.
- The proof stays scoped to `Thaw Window` and `Held Sand`, which matches the packet and avoids turning this pass into a broader route-HUD rewrite.

## Watch Item

The bubble accent line is now doing exactly the amount of work it should. Future follow-ons should keep this line to one short label plus slot text unless the bubble layout itself changes.
