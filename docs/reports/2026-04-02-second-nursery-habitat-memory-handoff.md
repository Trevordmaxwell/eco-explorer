# 2026-04-02 Second Nursery Habitat Memory Handoff

Prepared for `ECO-20260402-scout-127` in lane 2.

## Recommendation

Keep the second habitat-memory follow-on in the exact same mature `TEACHING BED` footer seam.

Do not add another nursery row, project state, extra summary, or journal detour.

The clean follow-on is to finish the nursery roster by authoring `memorySummary` for the remaining three projects:

- `dune-lupine-bed`
- `mountain-avens-bed`
- `beach-strawberry-bed`

## Why This Is The Right Follow-On

- The first pass already proved the seam. The mature-bed footer now carries place memory without widening the station.
- The remaining trio fills the missing habitat banding cleanly:
  - `dune-lupine-bed` for the brushier shelter shift behind the brighter dune face
  - `mountain-avens-bed` for low open alpine or fell ground
  - `beach-strawberry-bed` for the softer runner edge that makes the station feel more lived in
- Finishing the roster is smaller and safer than inventing a second memory surface for extras, route clues, or the bench card.

## Scope For `main-165`

- Add `memorySummary` only to the three remaining nursery projects.
- Keep the renderer unchanged unless one line truly overruns the current footer seam.
- Update the nursery test allowlist from the first three projects to the full six-project set.
- Capture at least one beauty-oriented proof (`beach-strawberry`) and one alpine route-support proof (`mountain-avens`) if possible.

## Guardrails

- Do not add another nursery card or text row.
- Do not widen the nursery shell.
- Do not change route-support or utility logic.
- Do not reopen save data.

## Outcome

- Close `ECO-20260402-scout-127`.
- Promote `ECO-20260402-main-165` to `READY`.
