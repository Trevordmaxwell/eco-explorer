# 2026-03-30 Coastal Front Richness Handoff

## Scope

Scout handoff for `ECO-20260330-scout-54`: prepare the first lane-2 content-richness pack for `beach`, `coastal-scrub`, and the coast-facing `forest` edge.

## Current Read

The coastal branch is structurally strong now:

- the `beach <-> coastal-scrub` seam teaches exposed dune to sturdier cover
- the `coastal-scrub <-> forest` seam now has a clear shade shift
- the route and corridor work already point the player at the right places

What is still lighter than the inland half is discovery density inside those habitats once the player is off the seam itself.

The best next move is not another travel or prompt pass. It is one compact authored content pack that deepens:

- washed-shore clues on the beach
- thorny thicket cover in coastal scrub
- berry-rich first-forest edge structure just inside `forest`

## Best Pack Shape

### 1. Add one strong new beach wash-up discovery

Best addition:

- `sand-dollar-test` in `tide-line` and light `tidepool` rotation

Why this is the right beach addition:

- it is visually distinctive and kid-readable
- it makes the beach feel richer without turning the coast into shell spam
- it is the strongest later close-look candidate from this pack

Teaching role:

- use it as a washed-up offshore clue, not a new dune-builder or wrack-food carrier

### 2. Add one shared thorny thicket carrier across scrub and forest

Best addition:

- `nootka-rose` as a shared entry between `coastal-scrub` and `forest`

Why this is the right shared carrier:

- it belongs in coastal thickets and forest-edge openings
- it creates a real future comparison candidate without opening a new system
- it deepens cover, nesting, and berry/hip shelter teaching in the front half

Placement guidance:

- `coastal-scrub`: `shrub-thicket` first, with a lighter touch in `forest-edge`
- `forest`: `trailhead` and early `fern-hollow`, so the coast-facing forest edge feels denser right away

### 3. Add one first-forest berry anchor

Best addition:

- `red-huckleberry` in the first forest half

Why this is the right forest-side addition:

- it is a strong Pacific forest-edge shrub
- it helps the forest start with more living variety instead of waiting for the deeper hollow content
- it pairs naturally with `nootka-rose`, `salmonberry`, and existing moisture/shrub teaching

Placement guidance:

- favor `trailhead` and `fern-hollow`
- keep `creek-bend` readable by not flooding the deeper forest with another shrub wave

## Ecosystem Note Targets

### Beach

Add one new tide-line note around washed clues.

Best note target:

- `sand-dollar-test`
- one shell entry such as `moon-snail-shell` or `native-littleneck-shell`
- `bull-kelp-wrack` or `sanderling`

Main lesson:

- waves leave both food and remains, and the tide line is full of clues from just offshore

### Coastal Scrub

Add one thorny-cover note keyed to:

- `nootka-rose`
- `song-sparrow`
- `deer-mouse`

Main lesson:

- thorny thickets change the scrub from open cover into safer nesting and hiding structure

### Forest

Add one coast-edge berry thicket note keyed to:

- `nootka-rose`
- `red-huckleberry`
- one existing forest edge carrier such as `douglas-fir-sapling` or `salmonberry`

Main lesson:

- the first forest edge is not only trees; it is also a layered shrub band built from filtered light, old wood, and cover

## What `main-86` Should Do

The cleanest implementation bundle is:

1. add `sand-dollar-test` to `beach`
2. add `nootka-rose` as a shared coastal-front entry across `coastal-scrub` and `forest`
3. add `red-huckleberry` to the first half of `forest`
4. add one new ecosystem note in each of the three biomes above
5. update the science ledger and content tests

## What `main-86` Should Not Do

- do not expand the comparison allowlist yet
- do not add new close-look payloads yet
- do not reopen corridor, route, station, or partner systems
- do not turn the beach pass into more wrack-only or bird-only content

## Best Later Candidates For `main-88`

Source-backed later targets from this coastal pack:

- close-look: `sand-dollar-test`
- comparison: `nootka-rose` if the scrub and forest notes both land cleanly

Existing coastal comparison entries such as `salmonberry` and `sword-fern` should remain in play, but this pass should focus first on making the new thicket/edge content real.

## Queue Guidance

- `ECO-20260330-scout-54` can close with this handoff.
- `ECO-20260330-main-86` should now move forward as a compact coastal discovery-and-note pack, not as a broader journal-system pass.
