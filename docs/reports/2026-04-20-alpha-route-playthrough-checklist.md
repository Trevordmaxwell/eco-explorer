# 2026-04-20 Alpha Route Playthrough Checklist

Implemented `ECO-20260420-main-329` for packet `130`.

## Purpose

This checklist is the lane-4 reviewer spine for the completed five-biome alpha arc. It maps the intended play path from a fresh save through filed `High Pass` without adding route behavior, new station UI, broader copy, geometry, support systems, or tests.

Use it as the human-readable contract before packet `131` adds debug instrumentation/snapshots and packet `133` builds the deterministic route-state matrix.

## Review Rules

- Start from a clean save unless a row explicitly calls for a seeded proof state.
- Treat every Route v2 outing as complete only after its station-side filing step, not after the final in-field inspect.
- Use support choice as a light readability check, not as a loadout or inventory system.
- Confirm `High Pass` filed state is a calm endpoint: no active `treeline-high-pass`, no `route-locator:treeline`, no route marker, and no `Today: High Pass` replay label.
- If a proof is listed as scattered, do not widen packet `130`; record the gap for packet `131` or packet `133`.

## Alpha Route Spine

| Step | Route State | Biome / Type | Player-Facing Completion | Support / Replay Check | Existing Proof Area | Gap / Handoff |
|---:|---|---|---|---|---|---|
| 1 | `beach-shore-shelter` / `Shore Shelter` | Sunny Beach / `transect-evidence` | Log `dune-grass -> lee-cover -> wrack-line`, then file the note at the station. | Late `wrack-hold` may reframe as `Wrack Shelter`; `hand-lens` should make the live clue easier to read without changing filed identity. | `field-requests.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover the ordered beach route, filing, and support-readable wrack-line shelf. | No single fresh-save checklist row existed before this report; packet `133` should later fold it into the route matrix. |
| 2 | `forest-hidden-hollow` / `Hidden Hollow` | Forest Trail / `landmark-evidence` | Confirm the seep stone in Root Hollow, then file the note. | `hand-lens` can help identify notebook-fit clues, but no special replay label is required. | `field-requests.test.ts` covers the route definition and ready/filed state; runtime filing helpers cover one-press station filing behavior. | Manual reviewers should still confirm the fresh beach-to-forest transition feels understandable until packet `131` named saves exist. |
| 3 | `forest-moisture-holders` / `Moisture Holders` | Forest Trail / `assemble-evidence` | Gather `shelter`, `ground`, and `living` clues, then file the note. | `Moist Hollow` can appear during active `moisture-hold`; `hand-lens` has live support proof on Root Hollow shelves. | `field-requests.test.ts`, `field-request-controller.test.ts`, and `runtime-smoke.test.ts` cover route state and support retarget behavior. | The proof is strong but distributed; packet `133` should name it in the all-route matrix. |
| 4 | `forest-survey-slice` / `Forest Survey` | Forest Trail / `survey-biome` | Reach surveyed state by logging four Forest Trail clues. | No Route v2 filing step; this is the bridge into the first station return. | `guided-field-season.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover the starter guidance and station-return handoff. | Packet `131` should provide a named mid-season save here. |
| 5 | Station return / `Trail Stride` | Field station / support unlock | Return to the station after the first forest chapter and advance toward the coast-to-forest route. | Support choice remains one tiny row; do not grow loadout UI. | `runtime-smoke.test.ts` covers first field-season guidance from starter note to next habitat pointer. | Manual reviewers should watch whether the route/support row is understandable without external instruction; packet `132` owns first-session wayfinding hardening. |
| 6 | `coastal-shelter-shift` / `Open To Shelter` | Coastal Scrub / `transect-evidence` | Log `open-bloom -> pine-cover -> edge-log`, then file the note. | `note-tabs` has a chapter-close exception once filed; `route-marker` should guide map intent only when selected. | `field-requests.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover the ordered transect and post-file page turn. | Packet `133` should check active, ready-to-file, filed, and note-tabs states in one matrix row. |
| 7 | `coastal-edge-moisture` / `Edge Moisture` | Coastal Scrub / `inspect-entry-set` | Inspect two cooler forest-edge moisture clues. | No station filing; this hands off to the inland shelter route. | `field-requests.test.ts` and `field-season-board.test.ts` cover unlock and board state. | Keep this smaller than the full `Open To Shelter` route; no new route shell is needed. |
| 8 | `treeline-stone-shelter` / `Stone Shelter` | Treeline Pass / `assemble-evidence` | Log `bent-cover -> stone-break -> lee-life`, then file the note. | Unlocks `place-tab`; support choice should remain route-facing and tiny. | `field-requests.test.ts`, `field-season-board.test.ts`, and support-row runtime coverage verify ordered route and support unlock behavior. | Packet `131` should expose a named save before/after `place-tab` unlock for easier review. |
| 9 | `tundra-short-season` / `Thaw Window` | Tundra Reach / `assemble-evidence` | Log `first-bloom -> wet-tuft -> brief-fruit`, then file the note. | Active `thaw-fringe` may use `woolly-lousewort` for `first-bloom` and `bigelows-sedge` for `wet-tuft`; `hand-lens` should make these choices player-felt. | `field-requests.test.ts`, `field-request-controller.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover `Thaw Window`, support retargeting, and filed identity stability. | Packet `133` should group the canonical `Short Season` filed identity with active `Thaw Window` state in one row. |
| 10 | `tundra-survey-slice` / `Tundra Survey` / `Bright Survey` | Tundra Reach / `survey-biome` | Reach surveyed state across Snow Meadow and Frost Ridge. | Peak phenology may reframe as `Bright Survey`; `place-tab` should use the `short-summer-rush` question. | `field-requests.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover world-state focus and place-tab behavior. | This row is a comprehension checkpoint for packet `132`, not a reason to add route behavior in packet `130`. |
| 11 | `scrub-edge-pattern` / `Scrub Pattern` / `Held Sand` | Coastal Scrub / `transect-evidence` | Log `open-pioneer -> holding-cover -> thicker-edge`, then file the note. | Late `sand-capture` may reframe as `Held Sand`; `beach-grass` can satisfy the active `open-pioneer` opportunity and `hand-lens` should make that felt. | `field-requests.test.ts`, `field-request-controller.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover `Held Sand`, support retargeting, and canonical filed identity. | Packet `133` should verify `Held Sand` active state does not rename the filed route. |
| 12 | `forest-cool-edge` / `Cool Edge` / `Moist Edge` | Forest Trail / `assemble-evidence` | Log `edge-carrier -> cool-floor -> wet-shade`, then file the note. | `Moist Edge` can appear during active moisture state; ordinary `hand-lens` retarget should be readable without using the stronger active-clue bubble unless warranted. | `field-request-controller.test.ts` and `runtime-smoke.test.ts` cover ordinary support-readable retargeting on this family. | The all-route matrix should distinguish ordinary retargets from active-clue alternates. |
| 13 | `treeline-low-fell` / `Low Fell` / `Brief Bloom` | Treeline Pass / `assemble-evidence` | Log `last-tree-shape -> low-wood -> fell-bloom -> low-rest`, then file the note. | Peak phenology can reframe the `fell-bloom` slot as `Brief Bloom`; support should stay in-field and compact. | `field-requests.test.ts`, `field-season-board.test.ts`, and runtime replay slices cover the ordered low-fell chapter and active-window carrier. | Packet `133` should verify low-fell order, active title, and filed title together. |
| 14 | `forest-expedition-upper-run` / `Root Hollow` | Forest Trail / `assemble-evidence` | Log `seep-mark -> stone-pocket -> root-held -> high-run`, then file the expedition note. | Expedition framing should still feel notebook-led, not like a second planner. | `field-requests.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover Root Hollow route and station filing. | Packet `131` should provide a named late-season save before Root Hollow filing. |
| 15 | `forest-season-threads` / `Season Threads` | Forest Trail / `inspect-entry-set` | Inspect floor cover, edge growth, and canopy-life clues, then return for the calm season close. | This is the bridge into dormant/active `High Pass`; `seasonCloseReturnPending` must suppress live `High Pass` until the return clears. | `field-requests.test.ts`, `field-season-board.test.ts`, and `runtime-smoke.test.ts` cover season-close return behavior. | Packet `133` should include both pending and cleared season-close states. |
| 16 | `treeline-high-pass` / `High Pass` / `Rimed Pass` | Treeline Pass / `assemble-evidence` | Log `stone-lift -> lee-watch -> rime-mark -> talus-hold`, return to station, and file `High Pass`. | Active `frost-rime` may reframe as `Rimed Pass`; `hand-lens` should prefer `reindeer-lichen` for `rime-mark` during that window, while filed state must clear route cues. | Packet `129` added the end-to-end runtime smoke from live talus-hold clue through station filing and settled filed state; `field-requests.test.ts` and `field-request-controller.test.ts` cover unit/controller state. | This is the completed alpha endpoint. Future packets may add proof/instrumentation, but should not reopen High Pass density or create a second route. |

## Route-State Proof Gap Summary

- The full fresh-save-to-filed-`High Pass` path is now documented here, but it is not yet a single automated route matrix.
- Existing proof is intentionally distributed across focused unit, controller, board, guided-season, and runtime-smoke slices.
- Packet `131` should add named debug snapshots or assertions for review starts such as fresh save, first station return, mid-season route-ready, High Pass active, ready-to-file, and filed.
- Packet `133` should turn this checklist into a deterministic route-state matrix across active, ready-to-file, filed, replay, support, and post-filed states.
- If external reviewers get lost before `Shore Shelter`, the follow-up belongs to packet `132`, not to lane-4 route behavior in packet `130`.

## Verification

- Documentation-only change.
- `npm run validate:agents` required after queue and packet updates.
