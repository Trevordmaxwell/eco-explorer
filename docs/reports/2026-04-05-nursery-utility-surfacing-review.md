# Nursery Utility Surfacing Review

## Verdict

Clean review. No blocking issue remains on the compost-notice utility follow-on.

## What I Checked

- the live `COMPOST HEAP` notice path in `src/engine/game.ts`
- the shared nursery state seam that exposes `utilityNote`
- the focused live runtime proof for bench, mature-bed, and crowberry-utility nursery states
- the rebuilt app after the shared `runtime-smoke` helper typing repair

## Why It Holds

- The payoff moved onto one existing intentional action instead of growing a new row, badge, or planner surface.
- The teaching bed stays focused on plant-state and reflection beats, so the home loop reads as separated moments rather than stacked copy.
- The compost fallback still exists for non-utility states, so the seam stays additive instead of replacing general nursery guidance.

## Watch Item

If later nursery utilities arrive, keep using authored compost-body lines instead of letting the bed or bench absorb those payoffs back into their denser card copy.
