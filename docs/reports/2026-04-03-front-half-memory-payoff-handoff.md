# 2026-04-03 Front-Half Memory Payoff Handoff

## Scope

Scout handoff for `ECO-20260403-scout-164`: narrow the front-half memory follow-on to one sketchbook-only note pass.

## Current Read

The front-half branch already has a strong first sketchbook layer:

- beach-side shared carriers already have authored note strips:
  - `beach-grass`
  - `sand-verbena`
  - `beach-pea`
  - `beach-strawberry`
- scrub and forest comparison carriers already have authored note strips:
  - `nootka-rose`
  - `salmonberry`

That means the next payoff should not invent a new atlas or station seam. The clearer gap is that two front-half anchors still fall back to plain fact text in the sketchbook:

- `sea-rocket`
- `sword-fern`

## Why These Two

### `sea-rocket`

- it is part of the front-half note cleanup and marks the most exposed starter edge where shelter has not fully built yet
- it currently has no authored sketchbook note, so the page falls back to a drier fact voice instead of a remembered field-note voice

### `sword-fern`

- it is one of the live front-half comparison anchors and one of the strongest “cooler, wetter ground begins here” carriers
- it also currently falls back to its short fact, which weakens the archived feel of the front-half comparison layer

## Recommended `main-202` Shape

Keep `main-202` compact:

1. add one authored `sketchbookNote` for `sea-rocket`
2. add one authored `sketchbookNote` for `sword-fern`
3. add focused `sketchbook` tests proving both new note strips surface through the live sketchbook page view
4. leave all sketchbook slots, layout, atlas copy, and comparison logic unchanged

## Why Not Atlas

- lane 2 guidance already favors strengthening sketchbook memory over reopening station or atlas shell copy
- the field-station atlas belongs closer to lane 1 continuity and route language work
- the current front-half memory gap is specifically in the note-strip language already shown on sketchbook pages

## What `main-202` Should Not Do

- do not add a new atlas card, station note, or archive panel
- do not change sketchbook layout, slots, or unlock logic
- do not widen the memory pass beyond these two note gaps
- do not reopen comparison allowlists or ecosystem-note resolution

## Queue Guidance

- `ECO-20260403-scout-164` can close with this handoff.
- `ECO-20260403-main-202` can move to `READY` once this report is linked.
