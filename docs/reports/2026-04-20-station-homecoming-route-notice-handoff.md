# Station Homecoming Route Notice Handoff

Created: 2026-04-20

## Queue Item

- Completed: `ECO-20260420-scout-345`
- Packet: `.agents/packets/134-station-homecoming-evolution-pass.json`
- Lane: `lane-4`

## Scout Finding

Lane 4 should not implement this packet until lane 1 lands and reviews the station homecoming seam. Lane 2's copy helper review has cleared, so the copy contract is ready for consumption, but the station-owned homecoming state is still the missing dependency.

The safest lane-4 chunk is a narrow notice/replay lifecycle pass after `ECO-20260420-critic-342` clears. It should make filed-route notices, support-selection notices, and world-map replay labels behave calmly around the new homecoming seam without adding station layout, copy, save state, route definitions, or geometry.

## Current Seam Facts

- Station route filing currently calls `fileReadyRouteV2FieldRequest(...)`, persists the save, then calls `showFieldRequestNotice(...)` with the route filed display text.
- `resolveRecordedFieldRequestNotice(...)` already gives Route v2 filings their canonical filed-route title, text, and `filed-route` variant.
- `drawFieldRequestNotice(...)` renders notices only while `overlayMode === 'playing'`, but `fieldRequestNotice` still exists in debug state and its timer ticks while the station overlay is open.
- `resolveFieldRequestState(...)` already clears active outing, route-marker, journal field-request, and `Today:` replay state after `treeline-high-pass` is filed; packet `133` added broader Route v2 matrix coverage for this invariant.
- `resolveFieldStationOpenState(...)` currently derives `arrivalMode: 'homecoming'` from station ledger/nursery/season-close sync, so the route-notice pass should consume lane 1's new homecoming seam instead of inventing another route-owned station state.

## Recommended Main Target

After the lane-1 seam is reviewed, add a small route-notice/homecoming handoff proof and the smallest behavior fix it requires.

Recommended behavior:

- Opening a homecoming station state should not let an older field-station guidance notice, support-selection notice, or route-replay notice reappear later as a stale toast.
- Filing a ready Route v2 note from the station should keep the filed-route notice as the only route notice generated by that action.
- Support selection should remain persisted, but support-selection toasts should not replace the filed-route notice during the same station interaction.
- When a route is filed, world-map `routeMarkerLocationId`, `routeReplayLabel`, active outing, and journal field-request state should clear or hand off to the next real outing, never to a stale `Today:` label for the filed route.
- If lane 1 exposes a homecoming milestone/debug state, consume that seam for the test; do not add a lane-4-specific station resolver.

## Recommended Files

- `src/engine/field-notices.ts` for any tiny pure notice-priority helper, if needed.
- `src/test/field-notices.test.ts` for direct notice-priority coverage, if a helper is added.
- `src/test/runtime-smoke.test.ts` for one focused station route-filing/homecoming smoke slice.
- `src/test/field-requests.test.ts` only if the stale `Today:` check is easier to prove in the pure field-request-state matrix.
- `src/engine/game.ts` only for the minimal notice lifecycle wiring after lane 1's seam exists.

## Acceptance For Main

- Remains blocked until `ECO-20260420-critic-342` clears; use lane 1's homecoming seam rather than adding a new station state.
- Respects the reviewed lane-2 copy helper contract and avoids changing homecoming copy.
- Adds or extends focused coverage for a ready-to-file Route v2 note filed from station during/around a homecoming return.
- Proves no stale `FIELD STATION`, support-selection, route-replay, or old `Today:` notice survives after homecoming opens or after the route is filed.
- Proves the filed-route notice uses the canonical route title/text and is not replaced by support-selection copy in the same interaction.
- Proves route-marker/replay/journal state clears for filed `treeline-high-pass`, or hands off to the next active outing for earlier route filings.
- Does not change station layout, station copy, homecoming copy, station shell visuals, route definitions, save schema, biome geometry, broader onboarding copy, or science content.

## Verification For Main

- Focused `runtime-smoke` slice covering the station filing/homecoming notice handoff.
- `npm test -- src/test/field-requests.test.ts` if the pure route-state matrix is touched.
- `npm test -- src/test/field-notices.test.ts` if a notice-priority helper is added.
- `npm run build` for runtime TypeScript changes.
- `npm run validate:agents`.
- `git diff --check`.

## Verification For Scout

- `npm run validate:agents` passed with the existing queue-size warning.
- `git diff --check` passed.
