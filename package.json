{
  "name": "eco-explorer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "science:check": "vitest run --run src/test/content-quality.test.ts",
    "validate:agents": "node scripts/validate-agent-packets.mjs",
    "alpha:rc": "node scripts/run-alpha-rc.mjs",
    "review:pack": "node scripts/create-review-drop.mjs",
    "review:verify": "node scripts/verify-review-drop.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vitest": "^4.1.2"
  }
}
